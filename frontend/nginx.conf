server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # API 요청은 FastAPI 컨테이너로 프록시
    location /api/ {
        proxy_pass http://backend:8000/;
    }
}
